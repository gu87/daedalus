[project]
name = "daedalus-orch"
version = "0.1.0"
description = "Daedalus orchestration layer — Python CLI and UDS client"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
daedalus = "daedalus.orch.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
